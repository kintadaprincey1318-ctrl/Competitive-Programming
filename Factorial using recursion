#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
long long factorial(int n) {
    if (n == 0 || n == 1)
        return 1;
    return n * factorial(n - 1);
}
int main() {
    int num;
    scanf("%d", &num);
    printf("%lld", factorial(num));
    return 0;
}

